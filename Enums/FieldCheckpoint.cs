using System;

namespace Aftership.Enums
{
    public enum FieldCheckpoint
    {
        created_at,
        checkpoint_time,
        city,
        coordinates,
        country_iso3,
        country_name,
        message,
        state,
        tag,
        zip
    }
}

